import 'package:flutter/material.dart';

class AlbumSreen extends StatelessWidget {
  const AlbumSreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Album'),
    );
  }
}
